# 发布指南：让别人一行命令装上这个技能

> 面向：你（发布者）
> 目标：把技能包传到网上，同学只需粘贴一行命令即可安装

---

## 一、先改一行（必做）

打开 [install.ps1](install.ps1)，找到 `param(...)` 里这一行：

```powershell
[string]$ZipUrl = "https://gitee.com/REPLACE_USER/REPLACE_REPO/raw/main/retrieval-exam-protocol.zip",
```

把里面的地址换成你自己的（第三步拿到后回来改）。

---

## 二、传到 Gitee（推荐）

**为什么推荐 Gitee 而不是 GitHub**：国内访问快且稳定，GitHub 的 raw 地址在国内经常超时——而下载失败是这套方案唯一的硬依赖。

1. 注册 [gitee.com](https://gitee.com)（手机号即可）
2. 右上角 `+` → **新建仓库**
   - 仓库名称：随便，如 `skill`
   - **开源**（必须公开，否则别人下不了）
   - 勾选「初始化仓库」
3. 进入仓库 → **上传文件** → 把这两个传上去：
   - `retrieval-exam-protocol.zip`
   - `install.ps1`
4. 点开 `retrieval-exam-protocol.zip` → 右上角**「原始数据」** → 复制地址栏的完整地址

   形如：
   ```
   https://gitee.com/你的用户名/skill/raw/master/retrieval-exam-protocol.zip
   ```

---

## 三、把地址填回脚本

用第二步拿到的地址替换 install.ps1 里那行 `$ZipUrl`，**保存并重新上传 install.ps1**（覆盖同名文件即可）。

然后点开 `install.ps1` → 「原始数据」→ 复制地址，这是你最终要发出去的**脚本地址**。

---

## 四、发给同学

> **一行命令安装（Windows PowerShell）**

```powershell
irm https://gitee.com/你的用户名/skill/raw/master/install.ps1 | iex
```

**就这一行。** 不用下载、不用解压、不用找隐藏文件夹——脚本会自己建目录、下载、解压、并把嵌套层级拍平。

装完提示 **"fully quit and reopen Claude Code"**，完全退出再打开就生效了。

**卸载**（同一行加参数）：
```powershell
irm https://gitee.com/你的用户名/skill/raw/master/install.ps1 | iex -Uninstall
```

---

## 关于 `irm | iex` 安全性

同学可能会问"这会不会执行恶意代码"。诚实的回答是：**会，`irm | iex` 本质就是下载并立即执行远程脚本**，这是这类一行安装命令的固有风险。

如果对方有顾虑，给他手动安装的路径（见下），或者让他先把脚本下载下来看一眼再执行。**不要为了推广而淡化这个风险。**

---

## 手动安装（不用脚本的兜底方案）

1. 下载 `retrieval-exam-protocol.zip`
2. 资源管理器地址栏粘贴 `%USERPROFILE%\.claude\skills` 回车（提示找不到就先建 `.claude` 再建 `skills`）
3. 把 zip 解压进去
4. **检查层级**：必须是 `skills\retrieval-exam-protocol\SKILL.md`，`SKILL.md` 不能再深一层。多了一层就把内层文件夹剪切出来
5. **完全退出再打开 Claude Code**

---

## Mac / Linux

脚本目前只有 Windows 版。Mac 上的等价命令是：

```bash
curl -fsSL <zip地址> -o /tmp/s.zip && mkdir -p ~/.claude/skills && \
  unzip -oq /tmp/s.zip -d /tmp/sx && \
  find /tmp/sx -name SKILL.md -exec dirname {} \; | head -1 | \
  xargs -I{} sh -c 'rm -rf ~/.claude/skills/retrieval-exam-protocol && cp -r "{}" ~/.claude/skills/'
```

要的话我写一个和 Windows 版同样健壮、带嵌套拍平的 `install.sh`。

---

## 发布前检查清单

- [ ] 仓库是**公开**的（私有仓库的 raw 地址别人访问会 403）
- [ ] `retrieval-exam-protocol.zip` 已上传，且「原始数据」地址能直接在浏览器打开
- [ ] `install.ps1` 里的 `$ZipUrl` 已改成真实地址
- [ ] 改完后**重新上传了** install.ps1
- [ ] 自己先用那行命令在一台干净机器上试装一次
- [ ] 确认技能内容里没有你不想公开的东西（**见下**）

---

## ⚠️ 发布前请先确认：内容脱敏

当前包里含有**院校特定信息**，公开前请判断是否要保留：

| 文件 | 含什么 | 建议 |
|---|---|---|
| `references/resource-routing-table.md` | 国防科技大学图书馆入口、CARSI 认证流程、远程账号系统、各库的具体登录要求 | 若只发给本校同学，没关系；若要公开传播，建议替换成通用描述 |
| `references/case-comparison-report.md` | 已脱敏，只有结构和统计 | 可直接发 |
| `SKILL.md` | 通用方法论，无院校信息 | 可直接发 |

你这套技能的价值在**方法论**（零检索给路径、机械停手信号、多选按概率取舍），不在具体的院校入口。所以**脱敏几乎不损失价值**——把 NUDT 专属那几行换掉，方法一个字都不用动。

要我出一版脱敏包（把路由表里的院校信息替换成中性描述）的话说一声。
